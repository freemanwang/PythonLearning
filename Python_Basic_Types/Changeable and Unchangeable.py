#对象在内存中保存，由3部分组成：
#   -id（标识）
#   -type（类型）
#   -value（值）       可变对象就是指这个value是可变的
#通过变量修改对象，修改的是其value
a = 10
print(f'a={a},id(a)={id(a)}')   #a=10,id(a)=140709409252464  多次运行，地址不变
a = 20
print(f'a={a},id(a)={id(a)}')   #a=20,id(a)=140709409252784  多次运行，地址不变
#可见，改变其值时，a的地址也变了

